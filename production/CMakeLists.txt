cmake_minimum_required(VERSION 2.8)
project( Production )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( Production Production.cpp )
target_link_libraries( Production ${OpenCV_LIBS} )